---
molecule:
  ignore_paths:
    - lib
    - bin
    - library
dependency:
  name: galaxy
driver:
  name: docker
docker:
  containers:
    - name: centos7
      image: milcom/centos7-systemd
      image_version: latest
      privileged: True
      ansible_groups:
        - role_test
verifier:
  name: goss
